# ServiceAccount for vibe-kanban-desktop pods
# Provides identity for pods to interact with Kubernetes API if needed
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vibe-kanban-desktop
  namespace: vibe
  labels:
    app: vibe-kanban-desktop
